# Versio Reverse — 設計書（草案 v0.2）

Noise Engineering Versio 用カスタムファームウエア「クロック同期リバース」の設計書です。
**本書は議論用の草案です。** 「未決事項」（§9）が確定するまで実装には着手しません。

---

## 1. 目的

- 入力音声をピッチ変化なしで逆再生する
- 外部クロック（2PPQN 想定）に同期して逆再生区間を区切り、**逆再生されたトランジェントをビートに合わせる**
- ツマミ（＋CV）でドライ音／リバース音をスイッチのように切り替える
- 逆再生時のトランジェント位置をツマミで前後に微調整できる

---

## 2. ハードウェア仕様（Versio / 調査結果）

| 項目 | 仕様 | 出典 |
|---|---|---|
| MCU | Electrosmith Daisy Seed（STM32H750, Cortex-M7 480MHz, FPU） | libDaisy |
| メモリ | SDRAM 64MB / QSPI Flash 8MB / 内部 SRAM 約1MB | NE ブログ |
| オーディオ | ステレオ入出力、24bit、既定 48kHz（最大 96kHz）、ブロックサイズ既定 48 サンプル（1ms） | `daisy_versio.cpp` |
| 入力レベル | 約 16Vpp でクリップ。R 入力は未接続時 L にノーマライズ | Desmodus Versio マニュアル |
| ツマミ | 7 個（`KNOB_0`〜`KNOB_6`）。**各ツマミは対応する CV 入力とアナログで加算され、1 本の ADC で読まれる** | libDaisy / マニュアル |
| CV 入力 | 7 系統、0〜5V。ツマミはオフセットとして働く（ツマミ値＋CV） | マニュアル |
| ゲート入力 | 1 系統（FSU ジャック、+2V 以上で High）。GPIO 読み取り（`hw.Gate()`） | libDaisy / マニュアル |
| ボタン | モーメンタリ 1 個（FSU/TAP ボタン、`hw.tap`）。ゲート入力とは別ピン | libDaisy |
| トグル | 3 ポジションスイッチ ×2（`hw.sw[0]`, `hw.sw[1]`） | libDaisy |
| LED | RGB LED ×4（PWM） | libDaisy |
| 書き込み | 背面 Daisy Seed の USB（DFU）。BOOT+RESET で DFU モード → Daisy Web Programmer か `make program-dfu` | NE ブログ / Daisy |

**要実機確認**
- `KNOB_n` とパネル上の物理位置の対応（公式ドキュメントに明記なし。Desmodus の並びは Blend / Tone / Regen / Speed / Index / Size / Dense）
- LED 0〜3 の物理位置

---

## 3. 機能仕様

### 3.1 全体方針：リバースエンジンは常時動作

リバースエンジンは **モードに関係なく常にバックグラウンドで動作** し、クロック同期・オフセット適用済みのリバース音を生成し続ける。
モード切替は「どちらの信号を出力するか」を選ぶだけである。

```
 常時: 入力 ──▶ [リングバッファ] ──▶ [クロック同期リバース（オフセット適用）] ──▶ Wet
 出力: モード = DRY ? 入力(Dry) : Wet     （切替時 2ms フェード）
```

これにより、ユーザーの切替操作がビートとずれていても、**切り替えた瞬間に聞こえるリバース音は常にビートに揃っている**（区間の途中から聞こえ始める）。

| モード | 出力 |
|---|---|
| DRY | 入力をそのまま出力（Dry 100% / Wet 0%） |
| REVERSE | リバース音のみ出力（Dry 0% / Wet 100%） |

- ミックス（クロスフェード）は持たない。スイッチのように切り替える
- クリック防止のため切替時のみ **2ms のフェード** を入れる（聴感上はスイッチ）
- DRY → REVERSE、REVERSE → DRY ともに即時

### 3.2 リバース方式（クロック同期リバース）

入力は常にリングバッファに録音し続ける。区間の開始時刻（クロック時刻）を `t_k` とすると、
次の区間開始までの間、**直前に録音された音を過去に向かって等速（×1.0）で読む**。

```
読み出し位置(t) = 2·t_k − t + d        （d = オフセット、§3.3）
```

- 読み出し速度は −1.0 倍固定 → **ピッチ変化なし**（整数サンプル読み出し、補間不要）
- 区間長 = 1 クロック間隔（2PPQN なので 8 分音符 1 つ分）
- 「直前の 1 クロック分の音」が逆順で再生され、区間頭のトランジェントは **次のクロックに向かって盛り上がり、次のクロックの位置に来る**
- 区間の切り替わりは新旧 2 つの読み出しヘッドを 5ms クロスフェードしてクリックを防ぐ
- 区間開始は「実クロック」または「仮想クロック」（§3.5）で行う

### 3.3 トランジェント合わせ用オフセット（-350ms〜+350ms）

| d | 挙動 | 実装 |
|---|---|---|
| d > 0（遅らせる） | 逆再生トランジェントが d だけ後ろにずれる | 区間の切替時刻を `t_k + d` にし、読み出し位置は上式のまま |
| d = 0 | トランジェントが次のクロック位置に来る | — |
| d < 0（早める） | 逆再生トランジェントが \|d\| だけ前にずれる | 読み出し窓を \|d\| だけ過去にずらす（区間はクロックで切替） |

- **±350ms の両方向とも実現可能**（どちらも過去の音だけで作れる）
- ツマミ中央 = 0ms。中央付近に ±3% 程度のデッドゾーンを設けて 0ms に合わせやすくする
- `d` は **区間開始時にラッチ** する（区間の途中で変えると読み出し位置が飛んでクリックになるため）
- 注意：`|d|` がクロック間隔を超える場合（2PPQN で BPM 約 86 以上 → 間隔 350ms 以下）、ずれ量は実質「`|d|` をクロック間隔で割った余り」として聞こえる（→ Q3）

### 3.4 バッファ長とメモリ

- **オーディオバッファ：6000ms**（6.0s × 48kHz × 2ch × 4byte ≈ 2.3MB、SDRAM に配置。64MB に対して十分余裕）
- 読み出し位置と書き込み位置の距離（遅れ）は最大 `2P + |d|`。
  区間長 P は §3.5 により最大 600ms に制限されるので、最大遅れは `2×600 + 350 = 1550ms` → 6000ms で十分

### 3.5 クロック処理

#### 前提
- 2PPQN、最低 BPM 50 → **最大有効クロック間隔 `T_MAX = 600ms`**
- ゲート入力をオーディオコールバック内で **サンプル単位でポーリング** し、立ち上がりをサンプル精度で検出
- 内部に「現在のクロック間隔 `T`」を持ち、実クロックが来ない間は **仮想クロック** を `T` ごとに発生させて区間を進める

#### ルール

| # | 状況 | 動作 |
|---|---|---|
| 1 | 実クロック受信（前回の実クロックからの間隔 Δ ≤ 600ms） | `T = Δ` に更新。この時刻から新しい区間を開始 |
| 2 | 実クロック受信（Δ > 600ms：クロック途絶からの復帰など） | `T` は据え置き。この時刻から新しい区間を開始（位相はリセット）。次の実クロックとの間隔でルール 1 により `T` を決め直す |
| 3 | 前回の区間開始から `T` 経過しても実クロックが来ない | 仮想クロックを発生し新しい区間を開始（最後の間隔で動作継続） |
| 4 | 仮想クロックの直後（`T` の 20% 以内）に実クロックが来た | テンポが少し遅くなっただけとみなし、区間開始を実クロック時刻に **付け直す**（極端に短い区間を作らない）。Δ はルール 1/2 で判定 |

- ルール 1・2 の組み合わせが「短い間隔なら最後と新しいクロックで設定し直し／長い間隔なら新しいクロックとその次で決め直す」に対応する
- 実クロックは常に仮想クロックより優先（実クロックが来た瞬間に位相を合わせる）
- 最短間隔：チャタリング対策として 2ms 以内の連続エッジは無視。間隔 20ms 未満は 20ms として扱う（案）

```
実クロック:  |    |    |    |              ×途絶×            |    |
仮想クロック:                  :    :    :    :    :
区間:        [ ][ ][ ][ ][ ][ ][ ][ ][ ][ ][ ][ ][ ][ ][ ][ ]
                             ↑最後の間隔 T で継続      ↑復帰: 位相リセット、次で T 再計測
```

### 3.6 モード切替（リバース ON/OFF）

- 切替ツマミ＋CV（加算値）が **50% を境に** DRY / REVERSE を切り替える
- CV・ツマミのノイズでばたつかないよう **ヒステリシス**（例: 52% で ON、48% で OFF）
- ツマミ 0 のとき CV が約 2.5V を超えると REVERSE（0/5V のゲート信号でそのまま切り替え可能）

---

## 4. パネル割り当て

今回の実装範囲は以下のみ。残りのツマミ・トグル・ボタンは **未使用**（将来の拡張用に確保）。

| コントロール | 機能 |
|---|---|
| KNOB_0 ＋ CV | REVERSE 切替（50% しきい値） |
| KNOB_1 ＋ CV | オフセット d（-350〜+350ms、中央 0） |
| FSU ゲート入力 | クロック入力（2PPQN） |
| KNOB_2〜6、SW_0/1、FSU ボタン | 未使用 |

※物理位置は実機確認後に決定（→ Q5）

### LED 表示（案）

| LED | 表示 |
|---|---|
| LED0 | モード（DRY=消灯、REVERSE=赤） |
| LED1 | 区間開始で点滅（実クロック=白、仮想クロック=黄） |
| LED2 | オフセット（負=青、0=消灯、正=緑、明るさ=量） |
| LED3 | 未使用（クロック未受信時は暗くオレンジ点灯など）|

---

## 5. 信号処理フロー

```
 In L/R ──┬───────────────────────────────────────────────┐
          │                                               │ (Dry)
          ▼                                               ▼
   [リングバッファ 6s, SDRAM]                       ┌──────────┐
          │                                        │ モード    │──▶ Out L/R
          ▼                                        │ 切替      │
   [読み出しヘッド A/B（逆方向×1.0）]──(Wet)─────▶│ (2ms fade)│
   [区間切替クロスフェード 5ms]                     └──────────┘
          ▲                                               ▲
 Gate ─▶ [立ち上がり検出] ─▶ [クロック管理(実/仮想)] ─▶ t_k, d ラッチ
                                                          │
 KNOB_0+CV ─▶ [ヒステリシス 48/52%] ──────────────────────┘
 KNOB_1+CV ─▶ [デッドゾーン → d]
```

- L/R は同じクロック・同じ読み出し位置で処理（ステレオリンク）
- リバースエンジン・クロック管理はモードに関係なく常時動作

---

## 6. ソフトウエア構成（案）

```
versio_test_1/
├── docs/design.md
├── src/
│   ├── main.cpp          # 初期化・オーディオコールバック・LED 更新
│   ├── reverse_engine.h  # リングバッファ・読み出しヘッド・クロスフェード（HW非依存）
│   ├── clock_tracker.h   # 立ち上がり検出・実/仮想クロック・間隔管理（HW非依存）
│   └── controls.h        # ツマミ→パラメータ変換（ヒステリシス、デッドゾーン）
├── lib/
│   ├── libDaisy/         # git submodule
│   └── DaisySP/          # git submodule（必要なら）
├── Makefile
└── .github/workflows/build.yml
```

- `reverse_engine` / `clock_tracker` はハードウェア非依存にし、PC 上で単体テストできるようにする
- コントロールの読み取り（`ProcessAnalogControls`）はオーディオコールバック内で行う

### リソース見積り

| 資源 | 使用量 | 余裕 |
|---|---|---|
| SDRAM | 約 2.3MB（6s ステレオ float） | 64MB 中 60MB 以上空き |
| CPU | 1 サンプルあたり 書き込み 1 + 読み出し 2 ヘッド×2ch + 数回の乗算 | 数 % 程度の見込み。将来機能（フィルタ、フィードバック、複数ヘッド等）を追加する余裕は十分 |
| Flash | libDaisy 込みで数十 KB | 128KB（内部）に収まる見込み |

---

## 7. ビルド・CI

- ツールチェーン: `arm-none-eabi-gcc`（Daisy Toolchain 相当）
- GitHub Actions: libDaisy をビルド → ファームウエアをビルド → `.bin` を Artifact としてアップロード
- 書き込み: Daisy Web Programmer（https://electro-smith.github.io/Programmer/）または `make program-dfu`

---

## 8. 制約・リスク

| リスク | 対策 |
|---|---|
| 区間境界・モード切替でのクリック | 5ms / 2ms の短いフェード |
| 入力のトランジェントがクロックより遅れる（楽器の立ち上がり等） | オフセット d で調整 |
| クロックのテンポ変化・ジッター | 実クロックで毎回位相リセット、間隔は毎回更新 |
| クロックが 1 発だけ抜けた場合、間隔が 2 倍（≤600ms）なら誤ってテンポ半分と解釈される | → Q2 |
| ADC ノイズで 50% 付近がばたつく | ヒステリシス |
| KNOB 番号と物理位置の対応が不明 | 実機確認（→ Q5） |

---

## 9. 未決事項

### 決定済み（v0.2）
- バッファ長 6000ms
- オフセット -350〜+350ms（両方向とも実現可能）
- リバースエンジンは常時動作し、モード切替は出力の選択のみ（切替操作がビートからずれても、リバース音はビートに揃う）
- クロック途絶時は最後の間隔で継続。間隔 ≤600ms（2PPQN・BPM50）なら更新、それより長ければ次のクロックとの間隔で決め直す
- 切替時 2ms フェード
- 追加機能は今回なし（リソースに余裕あり。今後検討）

### 未決
**Q1. 起動直後、クロックを 1 度も受信していないとき**
- (a) 既定の間隔（例: 500ms = BPM60 の 2PPQN）で仮想クロックを出して動作 ← 提案
- (b) 最初のクロックが来るまで Wet は無音
- (c) 最初のクロックが来るまで REVERSE でも原音を出す

**Q2. クロックが 1 発だけ抜けた場合**
例: BPM150（間隔 200ms）で 1 発抜けると次の間隔は 400ms になり、ルール上は「テンポが半分になった」と解釈されます。
- (a) このまま（ルールどおり。ケーブルの抜き差しなど長い途絶は正しく扱える） ← 提案（シンプル）
- (b) 仮想クロックが間に入った間隔は採用せず、実クロックが 2 回連続したときだけ間隔を更新（1 発抜けに強いが、急にテンポを半分にしたときの追従が 1 クロック遅れる）

**Q3. オフセットがクロック間隔を超える場合**
2PPQN で BPM 約 86 以上では 350ms がクロック間隔を超え、ずれ量が 1 区間分回り込みます。
- (a) ms 指定のまま（回り込みを許容） ← 提案（仕様どおり）
- (b) 実際のずれ量を ±(クロック間隔/2) に制限
- (c) ms ではなく「クロック間隔に対する割合（±50%）」で指定

**Q4. ステレオ / サンプルレート**
L/R リンク・48kHz でよいですか？ ← 提案

**Q5. ツマミの物理位置**
KNOB_0（切替）と KNOB_1（オフセット）をパネル上のどの位置に置きたいですか？
（KNOB の番号と位置の対応は実機確認が必要です。確認用の簡単なテストファームを先に作ることもできます）

**Q6. LED 表示** は §4 の案でよいですか？

---

## 10. 改版履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 0.1 | 2026-09-23 | 初版草案 |
| 0.2 | 2026-09-23 | バッファ 6000ms、リバースエンジン常時動作、クロック途絶・再同期ルール、追加機能なしを反映 |
